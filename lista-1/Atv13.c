Desenvolva um programa para calcular a distância entre dois pontos em um plano 2D.
Use as fórmulas: distância = sqrt((x2 - x1)^2 + (y2 - y1)^2) (utilize math.h para a função
sqrt()).

#include <stdio.h>
#include <math.h>
int main () {
    int x, y;

    sqrt((x2 - x1)^2 + (y2 - y1)^2);

}